# 生存档铺 - 2026-09-19 开发记录

## 1. 今天的目标

今天的核心目标是搭建背包/格子型 UI 的基础结构，确保后续可以继续扩展为背包、仓库、装备栏等系统，并且让格子能够按配置动态生成。

---

## 2. 已完成的内容

### 2.1 配置文件：InventoryConfig

新增了配置脚本，用于统一管理格子系统的基础参数。

文件：
- [Assets/_Project/Scripts/Config/InventoryConfig.cs](Assets/_Project/Scripts/Config/InventoryConfig.cs)

功能：
- columns：列数
- rows：行数
- cellSize：格子尺寸
- slotPrefab：格子预制体引用

这个配置文件的意义是：
- 让格子布局参数集中管理
- 后续改动格子数、大小时，只修改配置即可
- 为后续数据驱动 UI 算法打基础

---

### 2.2 格子生成逻辑：InventoryGridUI

新增了背包网格生成脚本，负责根据配置信息创建格子。

文件：
- [Assets/_Project/Scripts/Inventory/InventoryGridUI.cs](Assets/_Project/Scripts/Inventory/InventoryGridUI.cs)

当前功能：
- 清理旧格子
- 读取配置
- 设置 GridLayoutGroup 的 cellSize / 列数
- 计算 SlotContainer 的宽高
- 循环生成 slotPrefab 实例
- 根据 x、y 初始化格子坐标

核心逻辑：
- 一次生成整个网格
- 每个格子都会拿到自己的坐标
- 格子生成方式可复用到背包、仓库、装备槽等界面

目前这一版的核心思路是：
- 容器节点固定
- 格子预制体复用
- 使用代码动态生成，便于扩展

---

### 2.3 格子数据脚本：Slot

新增了格子类，用于记录每个格子在网格中的位置，并作为后续交互扩展的基础。

文件：
- [Assets/_Project/Scripts/Inventory/Slot.cs](Assets/_Project/Scripts/Inventory/Slot.cs)

当前功能：
- 记录 PosX / PosY
- 提供 Init(x, y) 初始化方法
- 预留鼠标进入/离开事件接口

这意味着后续可以很容易加上：
- 鼠标悬停提示
- 点击格子选中
- 拖拽物品
- 显示物品图标
- 判断格子是否占用

---

## 3. 目录整理

为了后续项目结构更清晰，今天把和背包/格子相关的脚本统一整理到了 Inventory 目录中：

- [Assets/_Project/Scripts/Inventory](Assets/_Project/Scripts/Inventory)
- [Assets/_Project/Scripts/Config](Assets/_Project/Scripts/Config)

这样处理的好处：
- 业务脚本和配置脚本分开
- 后续新增功能时更便于扩展
- 不会把所有脚本混在一个 UI 文件夹里

---

## 4. 目前的技术结论

### 4.1 自动换行

当前的格子生成实现依赖 GridLayoutGroup：
- 通过 FixedColumnCount 固定列数
- 通过 constraintCount 设置列数
- 多于列数时自动换行

也就是说，自动换行的关键点在于：
- GridLayoutGroup 存在于 SlotContainer 上
- cellSize 和 spacing 配置正确
- constraintCount 正确设置

---

### 4.2 代码生成 vs 手工拖拽

当前方案采用的是：
- 容器固定手动放置
- 格子使用预制体
- 通过代码 Instantiate 生成

这套方案适合于：
- 格子数量可变
- 需要动态刷新
- 后续要接数据驱动逻辑
- 需要复用同一个 slot 预制体

相比手工拖拽一大堆格子节点，代码生成更利于后期维护。

---

## 5. 当前问题 / 需要注意的事项

### 5.1 命名可以继续收紧

目前的脚本名称已经比较明确，但还可以进一步统一：
- Slot → InventorySlot
- InventoryGridUI 这个名字已比较合理

这样后续如果再有 EquipmentSlot、SkillSlot 等，就不会混淆。

### 5.2 预制体还没真正放到 Prefabs 目录里

目前脚本层已经整理完成，但格子预制体应当单独放到：
- Assets/_Project/Prefabs/Inventory

这样项目结构会更加标准。

### 5.3 还没做的逻辑

后续需要补齐：
- 物品图标显示
- 点击格子交互
- 拖拽逻辑
- 格子占用状态
- 背包数据和 UI 的绑定

---

## 6. 下一步计划

1. 将 Slot 类名调整为更明确的 InventorySlot
2. 将格子预制体放入 Prefabs/Inventory
3. 完成格子自动换行的校正和布局稳定性处理
4. 绑定物品数据结构，开始从配置/数据到 UI 的完整联动
5. 为背包系统做基础交互逻辑（点击、选中、拖拽）

---

## 7. 总结

今天的开发内容已经把背包格子系统的基础骨架搭起来了：
- 配置脚本已建立
- 网格生成脚本已实现
- 格子位置数据已记录
- 目录结构已初步整理

这意味着后续可以继续往“真正的背包系统”推进，而不是只停留在 UI 视觉层面。
