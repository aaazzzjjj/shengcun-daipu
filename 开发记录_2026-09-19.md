# 生存档铺 - 2026-09-19 开发记录

> 📌 2026-09-20 更新：项目已输出《[策划文档.md](策划文档.md)》，明确了完整玩法方向与开发阶段规划。本文档的"还没做的逻辑""下一步计划""总结"已对齐策划文档。

## 1. 今天的目标

今天的核心目标是搭建背包/格子型 UI 的基础结构，确保后续可以继续扩展为背包、仓库、装备栏等系统，并且让格子能够按配置动态生成。

---

## 2. 已完成的内容

### 2.1 配置文件：InventoryConfig

新增了配置脚本，用于统一管理格子系统的基础参数。

文件：
- [Assets/_Project/Scripts/Config/InventoryConfig.cs](Assets/_Project/Scripts/Config/InventoryConfig.cs)

功能：
- columns：列数
- rows：行数
- cellSize：格子尺寸
- slotPrefab：格子预制体引用

这个配置文件的意义是：
- 让格子布局参数集中管理
- 后续改动格子数、大小时，只修改配置即可
- 为后续数据驱动 UI 算法打基础

---

### 2.2 格子生成逻辑：InventoryGridUI

新增了背包网格生成脚本，负责根据配置信息创建格子。

文件：
- [Assets/_Project/Scripts/Inventory/InventoryGridUI.cs](Assets/_Project/Scripts/Inventory/InventoryGridUI.cs)

当前功能：
- 清理旧格子
- 读取配置
- 设置 GridLayoutGroup 的 cellSize / 列数
- 计算 SlotContainer 的宽高
- 循环生成 slotPrefab 实例
- 根据 x、y 初始化格子坐标

核心逻辑：
- 一次生成整个网格
- 每个格子都会拿到自己的坐标
- 格子生成方式可复用到背包、仓库、装备槽等界面

目前这一版的核心思路是：
- 容器节点固定
- 格子预制体复用
- 使用代码动态生成，便于扩展

---

### 2.3 格子数据脚本：Slot

新增了格子类，用于记录每个格子在网格中的位置，并作为后续交互扩展的基础。

文件：
- [Assets/_Project/Scripts/Inventory/Slot.cs](Assets/_Project/Scripts/Inventory/Slot.cs)

当前功能：
- 记录 PosX / PosY
- 提供 Init(x, y) 初始化方法
- 预留鼠标进入/离开事件接口

这意味着后续可以很容易加上：
- 鼠标悬停提示
- 点击格子选中
- 拖拽物品
- 显示物品图标
- 判断格子是否占用

---

### 2.4 挖空边框 Shader：UIHollowBorder

为了让背包面板有“空心边框”的视觉效果，新增了自定义 UI Shader 与配套材质。

文件：
- [Assets/_Project/Shaders/UI/UIHollowBorder.shader](Assets/_Project/Shaders/UI/UIHollowBorder.shader)
- [Assets/_Project/Materials/UI/HollowBorderMat.mat](Assets/_Project/Materials/UI/HollowBorderMat.mat)

功能：
- 通过 _BorderLeft / _BorderRight / _BorderTop / _BorderBottom 四个参数（0-1 比例）定义中间掏空区域
- 处于掏空区域内的像素直接 discard 丢弃，从而形成“空心”边框
- 保留了 UGUI 的 Stencil、裁剪（_ClipRect）、AlphaClip 等标准属性，兼容 Mask 与 UI 裁剪

用法：
- 给面板的边框 Image 换上 HollowBorderMat 材质
- 调整材质上的 Border 参数即可控制边框宽度

---

### 2.5 配置实例：InventoryConfig.asset

在配置脚本的基础上，创建了一个可直接拖入场景使用的配置资产实例。

文件：
- [Assets/_Project/Config/InventoryConfig.asset](Assets/_Project/Config/InventoryConfig.asset)

当前取值：
- columns = 24
- rows = 10
- cellSize = 32 x 32
- slotPrefab = ItemSlot 预制体

场景中的 InventoryGridUI 通过 config 字段引用这份资产，运行时会按这份配置生成 24 x 10 = 240 个格子。

---

## 3. 目录整理

为了后续项目结构更清晰，今天按用途把资源统一整理到了对应目录：

- [Assets/_Project/Scripts/UI](Assets/_Project/Scripts/UI) — 网格表现脚本（已由 Inventory 迁移）
- [Assets/_Project/Scripts/Config](Assets/_Project/Scripts/Config) — 配置脚本
- [Assets/_Project/Config](Assets/_Project/Config) — 配置资产实例
- [Assets/_Project/Prefabs/UI](Assets/_Project/Prefabs/UI) — UI 预制体
- [Assets/_Project/Materials/UI](Assets/_Project/Materials/UI) — UI 材质
- [Assets/_Project/Shaders/UI](Assets/_Project/Shaders/UI) — UI Shader
- [Assets/_Project/Art/UI](Assets/_Project/Art/UI) — UI 美术资源

这样处理的好处：
- 业务脚本和配置脚本分开
- 资源按类型归档（预制体 / 材质 / Shader / 美术）
- 后续新增功能时更便于扩展

---

## 4. 目前的技术结论

> 本节是可放进面试讲的技术点：每一条都对应「遇到的问题 → 选型取舍 → 落地做法」。

### 4.1 数据与表现分离：Model 层零 MonoBehaviour 依赖

背包的核心数据模型 `GridInventory`、`ItemInstance` 是**纯 C# 类**，不继承 `MonoBehaviour`，除 `UnityEngine` 基础类型（`Vector2Int` 等）外不引用任何游戏模块。

- 好处：放置/旋转/占用算法可以脱离 Unity 跑单元测试，不依赖场景、不依赖 GameObject。
- 面试可讲：为什么把「逻辑」和「表现」拆开——逻辑可测、可复用，表现层（`InventorySlot`）只做视图索引与高亮，占用真相永远在 Model 里，避免 UI 状态和数据状态两份真相打架。

### 4.2 统一格子模型：一套算法服务三种容器

仓库(24×10)、交易台(2×6)、客户窗口(2×6) 本质是同一种「矩形占格 + 旋转 + 重叠判断」结构，所以抽象成一个 `GridInventory`：

- `CanPlace(config, anchor, rotated)` 做越界 + 重叠双重校验；
- `TryPlace` / `Remove` / `FindItemAt` 统一入口。
- 面试可讲：识别到「多容器同构」时先抽象模型，而不是给每个容器复制一份放置逻辑；这是避免重复代码、保证行为一致的关键。

### 4.3 双索引结构：占用查询与物品定位各用所长

`GridInventory` 内部用**两套结构互补**：

- `bool[,] occupied` —— O(1) 判某格是否被占（拖拽高亮每帧都要查）；
- `Dictionary<Vector2Int, ItemInstance> items` —— 锚点格 O(1) 直接拿到物品实例。

- 面试可讲：数组换查询速度、字典换定位能力，各取所需；而不是只用一个 list 到处线性查找。
- 细节：`IsCellOccupied` 对**越界坐标返回 false**，让拖拽到格子外的悬停判断更省事——边界情况提前在底层兜住。

### 4.4 静态档案与运行时数据分离（ScriptableObject）

`ItemConfigSO`（ScriptableObject）只存**不变属性**（售价、品类、占格形状），`ItemInstance` 只存**动态状态**（位置、是否旋转、引用 config）。

- 好处：策划可在编辑器里改配置而不用动代码；运行时多份物品共享同一份 config，不重复存静态字段。
- 面试可讲：ScriptableObject 的「数据即资产」思想，以及为什么把「档案」和「实例」分开——单一数据源，改一处全局生效。

### 4.5 派生数据不存储：批发价自动计算

`ItemConfigSO.WholesalePrice => sellPrice × 0.8`，批发价**不单独存字段**，由固定售价推导。

- 面试可讲：单一数据源（Single Source of Truth）原则——派生值永远跟着源值走，避免「售价改了但批发价忘了同步」的脏数据。

### 4.6 轻量服务定位器 + 事件中心的取舍

- `GameServices`（静态服务定位器）替代分散的 MonoBehaviour 单例，所有 System 在 `Awake` 注册，其它模块按类型 `Get<T>()` 拿。
- `GameEvents`（静态事件中心）只做**跨模块广播**（余额变动、库存变动等），模块内部直接调用、不走事件，避免「事件满天飞」。
- 面试可讲：为什么不引入 VContainer/Zenject 这类重型 DI——单人小项目用静态定位器足够，收益与成本不匹配；以及「何时用事件、何时直接调用」的边界判断。

### 4.7 为什么用代码生成而不是手动摆格子

- 容器固定手动放置，格子用预制体 + 代码 `Instantiate` 生成。
- 理由：格子数量可变（240 格手动摆不现实）、需要动态刷新、后续接数据驱动逻辑、复用同一 slot 预制体。
- 面试可讲：对「会变」的东西用数据驱动（配置 + 代码），对「不变」的东西才手工固定。

### 4.8 自定义 UI Shader 的兼容性：挖空边框

`UIHollowBorder` 通过四个 0~1 参数定义中间掏空区域，内部 `discard` 掉中心像素实现空心边框；同时保留了 UGUI 的 Stencil、`_ClipRect` 裁剪、AlphaClip 等标准属性。

- 面试可讲：自定义 shader 要「融入 UGUI 体系」——Mask 遮罩、ScrollRect 裁剪靠 Stencil 和 clip rect，丢掉这些标准属性会让边框在 Mask/滚动裁剪下显示错乱。

---

## 5. 当前问题 / 需要注意的事项

### 5.1 命名已统一 ✅

- `Slot` → `InventorySlot`（已改名，文件迁至 `Scripts/UI/InventorySlot.cs`）
- `InventoryGridUI` 这个名字保持不变

### 5.2 预制体目录还可以更规范

格子预制体其实已经放到 Prefabs 目录里了（当前在 Prefabs/UI/ItemSlot.prefab），但更合理的归类是放到专门的 Inventory 子目录：
- Assets/_Project/Prefabs/Inventory

后续可以把 ItemSlot 从 Prefabs/UI 移到 Prefabs/Inventory，让 UI 目录只放通用 UI 组件。

### 5.3 还没做的逻辑

> 自 2026-09-20 起，项目已输出《策划文档.md》，明确完整玩法方向（经济、时间循环、顾客、价格波动、日报/小偷等）。以下"待实现"已按策划文档的改良 MVC 架构与阶段规划重新梳理。

后续需要补齐：
- **数据模型**：`ItemConfigSO`（物品静态档案）+ `ItemInstance`（运行时数据），暂定 4 种饮料（1×1、1×2、2×1、2×2）
- **核心逻辑**：`InventoryManager` 单例与 `CanPlace` / `TryPlace` 纯逻辑校验
- **交互**：`DragController` 拖拽、R 旋转、绿/红高亮与 UGUI 坐标转换
- **交易系统**：`TradeGridManager`（交易台 + 客户窗口）、顾客系统、租金结算与交易面板联动
- **经济系统**：信用点收支、周租金、睡前日报（电费/保险）、夜晚小偷、报纸价格波动

### 5.4 GUID 断裂（已修复 ✅）

> 因脚本曾被删除重建，`.meta` 的 GUID 已改变，导致场景/预制体中的旧引用断裂。已通过把脚本 `.meta` 的 GUID 改回引用处的稳定值修复。

| 脚本（迁移后） | 修复后 GUID（.meta） | 引用处 | 状态 |
|----------------|----------------------|--------|------|
| `UI/InventorySlot.cs`（原 `Slot.cs`） | `1bb8bb0f3d1d27943a1d517acec0286f` | `ItemSlot.prefab` | ✅ |
| `UI/InventoryGridUI.cs` | `1d14acf54acebe64395d4c3c85e2d8e0` | `Test.unity` | ✅ |

修复方式：将 `.meta` 的 GUID 改回 scene/prefab 引用处的旧值（脚本重建前的 GUID），并在文件迁移时保持 `.meta` 同步（GUID 不变），引用即自动恢复。

---

## 6. 下一步计划

> 2026-09-20 已输出《策划文档.md》，下列计划已对齐其"开发阶段规划"（第一阶段已完成，从第二阶段开始）。

1. **第二阶段**：`ItemConfigSO` + `ItemInstance`，搭饮料数据模型（暂定 4 种矩形饮料）
2. **第三阶段**：`InventoryManager` 单例与 `CanPlace` / `TryPlace` 纯逻辑校验
3. **第四阶段**：`DragController` 拖拽、R 旋转、绿/红高亮与 UGUI 坐标转换
4. **第五阶段**：`TradeGridManager`（交易台/客户窗口）、顾客系统、租金结算与交易面板联动
5. **收尾修正**：Slot 更名 `InventorySlot` ✅（已完成）、预制体移入 `Prefabs/Inventory`、尺寸对齐修正（见 5.1 / 5.2）

---

## 7. 总结

- **2026-09-19（代码）**：背包格子系统基础骨架已搭起——配置脚本、网格生成、格子位置数据、目录整理。
- **2026-09-20（策划）**：输出《策划文档.md》，明确完整玩法方向——生存经营模拟：周租金递增、信用点交易、顾客买卖、报纸价格波动、睡前日报（电费/保险）、夜晚小偷事件；并梳理出改良 MVC 架构与五阶段开发规划。

这意味着项目已从"UI 视觉骨架"推进到"有明确玩法目标的可规划开发"阶段，后续按策划文档的阶段规划逐项落地。
